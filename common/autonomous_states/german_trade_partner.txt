autonomy_state = {
	id = autonomy_german_trade_partner
	
	is_puppet = yes
	
	min_freedom_level = 0.50
	
	
	
	rule = {
		can_not_declare_war = no
		can_decline_call_to_war = yes
	}
	
	modifier = {
		
		
		extra_trade_to_overlord_factor = 1.0
		
		can_master_build_for_us = 1
		
	}
	
	ai_subject_wants_higher = {
		factor = 1.0
	}
	
	ai_overlord_wants_lower = {
		factor = 1.0
	}

	ai_overlord_wants_garrison = {
		always = no
	}

	allowed = {
		has_dlc = "Together for Victory"

		if = {
			limit = { has_dlc = "Death or Dishonor" }
			NOT = {
				OR = {
					original_tag = RAJ
					original_tag = AUS
					original_tag = CAN
					original_tag = NZL
					original_tag = SAF
					original_tag = MAN
				}
			}
		}
	}
	
	can_take_level = {
		
		#trigger here
	}

	can_lose_level = {
		#trigger here
	}
}